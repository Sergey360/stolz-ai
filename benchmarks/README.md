# Benchmarks

This directory contains the versioned fixture, paired routes, raw synthetic evidence, and generated reports for `context-selection-v1`.

```bash
npm run benchmark:check
```

The checked-in result is scoped to its synthetic fixture. It is not measured Codex or provider telemetry. Read [the benchmarking guide](../docs/benchmarking.md) before interpreting the numbers or adding a suite.
